# Deferred Work

## Deferred from: code review of spec-ux-1-design-system-tokens (2026-06-15)

- Mobile sidebar — sidebar hidden at ≤800px with no hamburger/slide-in alternative (pre-existing, new feature beyond scope)
- Nested scroll containers — `.content-area` + `.chat-log` both `overflow-y: auto` creates double scrollbar UX (pre-existing structural issue)
- Unqualified `h2` selector — global style on all `<h2>` instead of scoped class (pre-existing)
